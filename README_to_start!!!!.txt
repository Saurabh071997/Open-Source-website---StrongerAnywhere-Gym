Steps to be followed :
1. To better view the website, zoomout the browser pagge to 80%;

2. Steps for php connection:
	2.1   Create a database named "strongeranywhere";
	2.2   Create tables product, user, location within the database;
	2.3   Product table has attributes: id, pname, image, price.
	2.4   Location table has attribute: id, country, city , address.
	2.5   User table hs attributes : id, firstname, lastname, email, gender, mobile, password.
